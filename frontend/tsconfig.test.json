{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests run in the Node process, so they legitimately reach for its globals.
    "types": ["vite/client", "node"]
  },
  "include": ["src"],
  "exclude": []
}
