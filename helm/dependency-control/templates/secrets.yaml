apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dependency-control.fullname" . }}-secrets
  labels:
    {{- include "dependency-control.labels" . | nindent 4 }}
type: Opaque
stringData:
  secret-key: {{ .Values.backend.secrets.secretKey | quote }}
